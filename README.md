# Formula Editor Demo

Vite + React + TypeScript demo that integrates:
- @monaco-editor/react for formula editing UI
- HyperFormula as formula parse / compute engine
- Tailwind for basic styling

Run:
```
pnpm install
pnpm run dev
```
or with npm:
```
npm install
npm run dev
```
